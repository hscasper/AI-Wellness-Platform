# Backend API URL
# Local development: use your machine's local IP (not localhost) if testing on a device
EXPO_PUBLIC_API_URL=http://10.0.0.151:5085

# Dev mode: append userId as query param (bypasses gateway auth)
# Automatically true in __DEV__; set explicitly for production-like builds
EXPO_PUBLIC_DEV_MODE=true
