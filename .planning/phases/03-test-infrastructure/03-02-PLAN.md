---
phase: 03-test-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs
  - community-service/CommunityService.Tests/CommunityService.Tests.csproj
  - journal-service/src/JournalService.Tests/JournalService.Tests.csproj
  - journal-service/src/JournalService.Tests/Services/JournalEntryServiceTests.cs
  - journal-service/src/JournalService.Tests/Services/PatternAnalysisServiceTests.cs
  - journal-service/src/JournalService.Tests/Infrastructure/StoredProcedureValidationTests.cs
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "Community-service has tests for post CRUD controller actions and reaction endpoints"
    - "Journal-service has a new test project with tests for JournalEntryService and PatternAnalysisService"
    - "Journal-service StoredProcedureExecutor validation is tested in the journal-service test project"
    - "All community-service and journal-service tests pass"
  artifacts:
    - path: "community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs"
      provides: "Controller unit tests for community post CRUD and reactions"
      contains: "class CommunityControllerTests"
    - path: "journal-service/src/JournalService.Tests/JournalService.Tests.csproj"
      provides: "New xUnit test project for journal-service"
      contains: "IsTestProject"
    - path: "journal-service/src/JournalService.Tests/Services/JournalEntryServiceTests.cs"
      provides: "Unit tests for journal entry CRUD operations"
      contains: "class JournalEntryServiceTests"
  key_links:
    - from: "journal-service/src/JournalService.Tests/JournalService.Tests.csproj"
      to: "journal-service/src/JournalService.Api/JournalService.Api.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*JournalService.Api"
---

<objective>
Create community-service controller tests and a new journal-service test project with service-layer tests.

Purpose: TEST-02 requires community-service tests beyond the existing GatewayAuthMiddleware tests -- adding post CRUD and reaction controller coverage. TEST-03 requires a brand-new test project for journal-service covering journal CRUD and pattern analysis.
Output: CommunityControllerTests.cs with post/reaction tests, JournalService.Tests project with JournalEntryServiceTests and PatternAnalysisServiceTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-infrastructure/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create journal-service test project and write JournalEntryService + PatternAnalysisService tests</name>
  <files>
    journal-service/src/JournalService.Tests/JournalService.Tests.csproj,
    journal-service/src/JournalService.Tests/Services/JournalEntryServiceTests.cs,
    journal-service/src/JournalService.Tests/Services/PatternAnalysisServiceTests.cs,
    journal-service/src/JournalService.Tests/Infrastructure/StoredProcedureValidationTests.cs
  </files>
  <read_first>
    journal-service/src/JournalService.Api/JournalService.Api.csproj,
    journal-service/src/JournalService.Api/Services/JournalEntryService.cs,
    journal-service/src/JournalService.Api/Services/PatternAnalysisService.cs,
    journal-service/src/JournalService.Api/Services/DatabaseService.cs,
    journal-service/src/JournalService.Api/Infrastructure/StoredProcedureExecutor.cs,
    auth-service/AuthService.Tests/AuthService.Tests.csproj
  </read_first>
  <action>
    1. Create `journal-service/src/JournalService.Tests/JournalService.Tests.csproj` using auth-service test project as template:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <TargetFramework>net9.0</TargetFramework>
           <ImplicitUsings>enable</ImplicitUsings>
           <Nullable>enable</Nullable>
           <IsPackable>false</IsPackable>
           <IsTestProject>true</IsTestProject>
         </PropertyGroup>
         <ItemGroup>
           <PackageReference Include="coverlet.collector" Version="6.0.2" />
           <PackageReference Include="FluentAssertions" Version="8.0.0" />
           <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
           <PackageReference Include="Moq" Version="4.20.72" />
           <PackageReference Include="xunit" Version="2.9.2" />
           <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
         </ItemGroup>
         <ItemGroup>
           <Using Include="Xunit" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="../JournalService.Api/JournalService.Api.csproj" />
         </ItemGroup>
       </Project>
       ```

    2. Read `JournalEntryService.cs` to discover its constructor dependencies (likely `DatabaseService` or `StoredProcedureExecutor`), public methods, and return types. Then create `JournalEntryServiceTests.cs`:
       - Mock all constructor dependencies
       - Test: CreateEntry with valid data calls the database layer and returns entry
       - Test: GetEntries returns list from database
       - Test: GetEntryById with non-existent ID returns null or throws
       - Use Moq for mocking, xUnit [Fact] for test methods

    3. Read `PatternAnalysisService.cs` to discover its interface and methods. Create `PatternAnalysisServiceTests.cs`:
       - Mock dependencies
       - Test: AnalyzePatterns with valid journal entries returns analysis result
       - Test: AnalyzePatterns with empty entries returns empty/default result

    4. Create `Infrastructure/StoredProcedureValidationTests.cs` — move the journal-specific stored procedure validation tests from auth-service.Tests to this project. The regex pattern `^[a-zA-Z_][a-zA-Z0-9_]*\z` should be tested here since journal-service has its own StoredProcedureExecutor. Test:
       - Valid function names pass validation (e.g., "get_entries", "create_entry")
       - SQL metacharacters are rejected (semicolons, quotes, spaces, parentheses)
       - Empty string is rejected
       - Trailing newline is rejected

    IMPORTANT: Read the actual source files before writing tests. The exact method names, parameter types, and dependency interfaces must match what is in the codebase. Do NOT guess method signatures.
  </action>
  <verify>
    <automated>cd E:/Capstone/Code/AI-Wellness-Platform && dotnet build journal-service/src/JournalService.Tests/ -q && dotnet test journal-service/src/JournalService.Tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - journal-service/src/JournalService.Tests/JournalService.Tests.csproj contains `<IsTestProject>true</IsTestProject>`
    - journal-service/src/JournalService.Tests/JournalService.Tests.csproj contains `ProjectReference` to `JournalService.Api.csproj`
    - journal-service/src/JournalService.Tests/Services/JournalEntryServiceTests.cs contains `class JournalEntryServiceTests`
    - journal-service/src/JournalService.Tests/Services/PatternAnalysisServiceTests.cs contains `class PatternAnalysisServiceTests`
    - journal-service/src/JournalService.Tests/Infrastructure/StoredProcedureValidationTests.cs contains `ValidateFunctionName` or stored procedure regex test
    - `dotnet test journal-service/src/JournalService.Tests/` exits 0
  </acceptance_criteria>
  <done>Journal-service test project exists with passing tests for JournalEntryService, PatternAnalysisService, and StoredProcedureExecutor validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add community-service controller tests for post CRUD and reactions</name>
  <files>
    community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs,
    community-service/CommunityService.Tests/CommunityService.Tests.csproj
  </files>
  <read_first>
    community-service/CommunityService/Controllers/CommunityController.cs,
    community-service/CommunityService/CommunityService.csproj,
    community-service/CommunityService.Tests/CommunityService.Tests.csproj,
    community-service/CommunityService.Tests/Middleware/GatewayAuthMiddlewareTests.cs
  </read_first>
  <action>
    1. Read `CommunityController.cs` to discover:
       - Constructor dependencies (likely a database service or repository)
       - Action methods for post CRUD (Create, Read, Update, Delete)
       - Action methods for reactions (like/unlike or similar)
       - How user identity is extracted (HttpContext.Items["AuthenticatedUser"] or similar)

    2. If needed, add `Moq` or `FluentAssertions` to `CommunityService.Tests.csproj` (check if already present).

    3. Create `community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs`:
       - Mock all controller constructor dependencies
       - Set up `DefaultHttpContext` with `Items["AuthenticatedUser"]` or the appropriate user context mechanism
       - Test: CreatePost with valid data returns 201/200
       - Test: GetPosts returns list
       - Test: AddReaction with valid post ID returns success
       - Test: CreatePost without user context returns 401 or throws

    IMPORTANT: Read the actual CommunityController.cs source to determine exact method names and signatures. The controller may use `IUserContext`, `HttpContext.Items`, or claims. Match the actual pattern.
  </action>
  <verify>
    <automated>cd E:/Capstone/Code/AI-Wellness-Platform && dotnet test community-service/CommunityService.Tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs contains `class CommunityControllerTests`
    - community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs contains at least one `[Fact]` attribute
    - community-service/CommunityService.Tests/Controllers/CommunityControllerTests.cs tests post creation (contains `Create` or `Post` in a test name)
    - `dotnet test community-service/CommunityService.Tests/` exits 0
  </acceptance_criteria>
  <done>Community-service has controller tests covering post CRUD and reaction operations. All tests pass alongside existing GatewayAuthMiddleware tests.</done>
</task>

</tasks>

<verification>
1. `dotnet test journal-service/src/JournalService.Tests/` passes with at least 5 tests
2. `dotnet test community-service/CommunityService.Tests/` passes including both middleware and controller tests
3. `dotnet build journal-service/src/JournalService.Api/` still succeeds (no production code broken)
</verification>

<success_criteria>
- Journal-service test project created with csproj, project reference, and IsTestProject=true
- JournalEntryServiceTests has passing tests for CRUD operations
- PatternAnalysisServiceTests has passing tests for analysis
- StoredProcedureValidationTests in journal-service tests SQL metacharacter rejection
- CommunityControllerTests has passing tests for post CRUD and reactions
- All tests pass in both projects
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure/03-02-SUMMARY.md`
</output>
