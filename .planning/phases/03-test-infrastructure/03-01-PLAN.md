---
phase: 03-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - auth-service/Services/Abstraction/IPasswordHasher.cs
  - auth-service/Services/BcryptPasswordHasher.cs
  - auth-service/Services/AuthService.cs
  - auth-service/Program.cs
  - auth-service/AuthService.Tests/AuthService.Tests.csproj
  - auth-service/AuthService.Tests/Services/PasswordHasherTests.cs
  - auth-service/AuthService.Tests/Services/AuthServiceTests.cs
autonomous: true
requirements: [TEST-08, TEST-01]
must_haves:
  truths:
    - "AuthService no longer calls BCrypt.Net.BCrypt directly — all password hashing goes through IPasswordHasher"
    - "BcryptPasswordHasher wraps BCrypt.Net.BCrypt and implements IPasswordHasher"
    - "AuthServiceTests mock IPasswordHasher instead of calling BCrypt directly"
    - "Tests exist for GetUserInfo authorization, duplicate-email error shape, and CSPRNG code generation"
    - "All auth-service tests pass with dotnet test"
  artifacts:
    - path: "auth-service/Services/Abstraction/IPasswordHasher.cs"
      provides: "IPasswordHasher interface with HashPassword and VerifyHashedPassword methods"
      contains: "interface IPasswordHasher"
    - path: "auth-service/Services/BcryptPasswordHasher.cs"
      provides: "BCrypt wrapper implementing IPasswordHasher"
      contains: "class BcryptPasswordHasher : IPasswordHasher"
    - path: "auth-service/AuthService.Tests/Services/PasswordHasherTests.cs"
      provides: "Unit tests for BcryptPasswordHasher"
      contains: "class PasswordHasherTests"
  key_links:
    - from: "auth-service/Services/AuthService.cs"
      to: "auth-service/Services/Abstraction/IPasswordHasher.cs"
      via: "constructor injection"
      pattern: "IPasswordHasher _passwordHasher"
    - from: "auth-service/Program.cs"
      to: "auth-service/Services/BcryptPasswordHasher.cs"
      via: "DI registration"
      pattern: "AddScoped<IPasswordHasher, BcryptPasswordHasher>"
---

<objective>
Introduce IPasswordHasher abstraction into auth-service and expand auth-service test coverage.

Purpose: TEST-08 extracts BCrypt calls behind an interface so AuthService can be unit tested with mocked password hashing. TEST-01 expands auth-service tests to cover GetUserInfo authorization, duplicate-email error shape, and ensures all existing tests pass with the new IPasswordHasher injection.
Output: IPasswordHasher interface, BcryptPasswordHasher implementation, updated AuthService constructor, PasswordHasherTests, and updated AuthServiceTests with mock IPasswordHasher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-infrastructure/03-RESEARCH.md

<interfaces>
<!-- Existing auth-service abstractions (folder is Abstraction, singular) -->

From auth-service/Services/Abstraction/IAuthService.cs:
```csharp
namespace AIWellness.Auth.Services.Abstractions;
public interface IAuthService { ... }
```

From auth-service/Services/AuthService.cs:
```csharp
namespace AIWellness.Auth.Services;
public class AuthService : IAuthService
{
    // Constructor takes: IUserRepository, IPasswordValidator, INotificationService, IJwtService, IHttpContextAccessor, IConfiguration, ILogger<AuthService>
    // BCrypt calls at lines 67, 109, 234, 241, 258, 261, 264
}
```

From auth-service/AuthService.Tests/Services/AuthServiceTests.cs:
```csharp
// Uses BCrypt.Net.BCrypt.HashPassword("ValidPass1!") at lines 153 and 245 for test setup
// CreateSut() wires all mocked dependencies into new AuthService(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IPasswordHasher interface, BcryptPasswordHasher, and wire into AuthService + DI</name>
  <files>
    auth-service/Services/Abstraction/IPasswordHasher.cs,
    auth-service/Services/BcryptPasswordHasher.cs,
    auth-service/Services/AuthService.cs,
    auth-service/Program.cs,
    auth-service/AuthService.Tests/Services/PasswordHasherTests.cs
  </files>
  <read_first>
    auth-service/Services/AuthService.cs,
    auth-service/Services/Abstraction/IAuthService.cs,
    auth-service/Services/Abstraction/IPasswordValidator.cs,
    auth-service/Program.cs,
    auth-service/AuthService.Tests/Services/AuthServiceTests.cs
  </read_first>
  <behavior>
    - Test: BcryptPasswordHasher.HashPassword returns a non-null BCrypt hash string (starts with "$2")
    - Test: BcryptPasswordHasher.VerifyHashedPassword returns true for correct password
    - Test: BcryptPasswordHasher.VerifyHashedPassword returns false for wrong password
  </behavior>
  <action>
    1. Create `auth-service/Services/Abstraction/IPasswordHasher.cs`:
       ```csharp
       namespace AIWellness.Auth.Services.Abstractions;
       public interface IPasswordHasher
       {
           string HashPassword(string password);
           bool VerifyHashedPassword(string hashedPassword, string providedPassword);
       }
       ```
       NOTE: The namespace is `AIWellness.Auth.Services.Abstractions` (with s) matching existing interfaces. The FOLDER is `Abstraction` (no s) — this is an existing codebase inconsistency, do NOT rename the folder.

    2. Create `auth-service/Services/BcryptPasswordHasher.cs`:
       ```csharp
       namespace AIWellness.Auth.Services;
       public sealed class BcryptPasswordHasher : Abstractions.IPasswordHasher
       {
           public string HashPassword(string password) =>
               BCrypt.Net.BCrypt.HashPassword(password);
           public bool VerifyHashedPassword(string hashedPassword, string providedPassword) =>
               BCrypt.Net.BCrypt.Verify(providedPassword, hashedPassword);
       }
       ```

    3. Update `auth-service/Services/AuthService.cs`:
       - Add `private readonly IPasswordHasher _passwordHasher;` field
       - Add `IPasswordHasher passwordHasher` parameter to constructor (after IPasswordValidator)
       - Replace `BCrypt.Net.BCrypt.HashPassword(...)` calls (lines ~67, 241, 264) with `_passwordHasher.HashPassword(...)`
       - Replace `BCrypt.Net.BCrypt.Verify(a, b)` calls (lines ~109, 234, 258, 261) with `_passwordHasher.VerifyHashedPassword(b, a)` — note BCrypt.Verify takes (plain, hash) but IPasswordHasher takes (hash, plain), so swap argument order

    4. Register in DI in `auth-service/Program.cs`:
       Add `builder.Services.AddScoped<AIWellness.Auth.Services.Abstractions.IPasswordHasher, AIWellness.Auth.Services.BcryptPasswordHasher>();` alongside existing service registrations.

    5. Create `auth-service/AuthService.Tests/Services/PasswordHasherTests.cs` with 3 tests (see behavior block).

    6. Update `auth-service/AuthService.Tests/Services/AuthServiceTests.cs`:
       - Add `private readonly Mock<AIWellness.Auth.Services.Abstractions.IPasswordHasher> _passwordHasher = new();` field
       - Add `_passwordHasher.Object` to CreateSut() constructor call
       - Replace `BCrypt.Net.BCrypt.HashPassword("ValidPass1!")` in test setup with a static hash string `"$2a$11$fakehashfortesting"` and configure `_passwordHasher.Setup(p => p.VerifyHashedPassword(It.IsAny<string>(), "ValidPass1!")).Returns(true)` where login tests need valid passwords
       - For locked account test and other tests that check password-related paths, set up the mock to return true/false as needed
  </action>
  <verify>
    <automated>cd E:/Capstone/Code/AI-Wellness-Platform && dotnet build auth-service/AuthService.csproj -q && dotnet test auth-service/AuthService.Tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - auth-service/Services/Abstraction/IPasswordHasher.cs contains `interface IPasswordHasher`
    - auth-service/Services/Abstraction/IPasswordHasher.cs contains `string HashPassword(string password)`
    - auth-service/Services/Abstraction/IPasswordHasher.cs contains `bool VerifyHashedPassword(string hashedPassword, string providedPassword)`
    - auth-service/Services/BcryptPasswordHasher.cs contains `class BcryptPasswordHasher : Abstractions.IPasswordHasher` or `class BcryptPasswordHasher : IPasswordHasher`
    - auth-service/Services/AuthService.cs contains `IPasswordHasher _passwordHasher`
    - auth-service/Services/AuthService.cs does NOT contain `BCrypt.Net.BCrypt.HashPassword` or `BCrypt.Net.BCrypt.Verify`
    - auth-service/Program.cs contains `IPasswordHasher` and `BcryptPasswordHasher`
    - auth-service/AuthService.Tests/Services/PasswordHasherTests.cs contains `class PasswordHasherTests`
    - auth-service/AuthService.Tests/Services/AuthServiceTests.cs contains `Mock<` and `IPasswordHasher`
    - `dotnet test auth-service/AuthService.Tests/` exits 0 with all tests passing
  </acceptance_criteria>
  <done>IPasswordHasher interface and BcryptPasswordHasher exist, AuthService uses IPasswordHasher via DI (zero direct BCrypt calls remain), PasswordHasherTests and updated AuthServiceTests all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Expand auth-service tests — GetUserInfo authorization guard and duplicate-email error shape</name>
  <files>
    auth-service/AuthService.Tests/Services/AuthServiceTests.cs
  </files>
  <read_first>
    auth-service/Services/AuthService.cs,
    auth-service/Controllers/AuthController.cs,
    auth-service/Exceptions/AuthConflictException.cs,
    auth-service/AuthService.Tests/Services/AuthServiceTests.cs
  </read_first>
  <action>
    Add the following test cases to `AuthServiceTests.cs`:

    1. **RegisterAsync_DuplicateEmail_ThrowsAuthConflictException_WithEmailExistsCode**:
       - Arrange: `_userRepository.Setup(r => r.GetByEmailAsync("dupe@test.com")).ReturnsAsync(new User { Id = Guid.NewGuid(), Email = "dupe@test.com", Username = "user", PasswordHash = "$2a$11$fake" })`
       - Act: Call `sut.RegisterAsync(new RegisterRequest { Email = "dupe@test.com", Username = "newuser", Password = "ValidPass1!" })` where `_passwordValidator` returns `(true, null)` and `_passwordHasher` returns any hash
       - Assert: `var ex = await Assert.ThrowsAsync<AuthConflictException>(...)` and `Assert.Equal("EMAIL_EXISTS", ex.ErrorCode)`

    2. **RegisterAsync_DuplicateEmail_ErrorShape_HasErrorCode**:
       - Same as above but also assert `ex.Message` contains "Email already registered"

    3. **GetUserInfoAsync_Returns_UserData_ForAuthenticatedUser** (if GetUserInfo method exists on IAuthService — check the actual method signature in AuthService.cs, it may be `GetUserInfoAsync` or inline in the controller):
       - If the method is in AuthController directly (reading user claims), write a controller-level test in AuthControllerTests.cs instead
       - The test must verify that the authenticated user's ID from claims matches the returned user data

    NOTE: Review AuthController.cs to find the GetUserInfo endpoint implementation. The `/user-info/{email}` endpoint was removed in Phase 1. The remaining endpoint may be `/user-info` (returns current user's info from JWT claims). Write the test based on the actual implementation found.
  </action>
  <verify>
    <automated>cd E:/Capstone/Code/AI-Wellness-Platform && dotnet test auth-service/AuthService.Tests/ -q --filter "DuplicateEmail|UserInfo"</automated>
  </verify>
  <acceptance_criteria>
    - auth-service/AuthService.Tests/Services/AuthServiceTests.cs contains `DuplicateEmail` in a test method name
    - auth-service/AuthService.Tests/Services/AuthServiceTests.cs contains `EMAIL_EXISTS`
    - auth-service/AuthService.Tests/Services/AuthServiceTests.cs contains `AuthConflictException`
    - `dotnet test auth-service/AuthService.Tests/ -q` exits 0
  </acceptance_criteria>
  <done>Auth-service test suite includes duplicate-email error shape test asserting EMAIL_EXISTS error code, and a GetUserInfo authorization test. All tests pass.</done>
</task>

</tasks>

<verification>
1. `dotnet build auth-service/AuthService.csproj` succeeds with 0 errors
2. `grep -rn "BCrypt.Net.BCrypt" auth-service/Services/AuthService.cs` returns 0 matches
3. `dotnet test auth-service/AuthService.Tests/` passes all tests (existing + new)
4. `grep -c "IPasswordHasher" auth-service/Services/AuthService.cs` returns at least 2 (field + constructor param)
</verification>

<success_criteria>
- IPasswordHasher interface exists with HashPassword and VerifyHashedPassword methods
- BcryptPasswordHasher wraps BCrypt.Net and is registered in DI
- AuthService.cs contains zero direct BCrypt.Net calls
- PasswordHasherTests has at least 3 passing tests
- AuthServiceTests mocks IPasswordHasher instead of calling BCrypt directly
- Duplicate-email error shape test asserts EMAIL_EXISTS code
- All auth-service tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-infrastructure/03-01-SUMMARY.md`
</output>
